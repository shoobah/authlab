import React from 'react';

const styles = {
    root: {
        padding: 16
    }
};

export function SigninPage() {
    return (
        <div style={styles.root}>
            <h1>Signing in...</h1>
        </div>
    );
}